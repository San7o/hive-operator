apiVersion: hive.dynatrace.com/v1alpha1
kind: HiveData
metadata:
  labels:
    app.kubernetes.io/name: hive-operator
    app.kubernetes.io/managed-by: kustomize
  name: hivedata-sample
spec:
  # TODO(user): Add fields here
