apiVersion: v1
kind: Service
metadata:
  name: callback-service
  namespace: hive-operator-system
spec:
  selector:
    app.kubernetes.io/name: callback-pod
  ports:
    - protocol: TCP
      port: 9376
      targetPort: 8090
